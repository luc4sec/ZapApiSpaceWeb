/* const cronJob = require('cron').CronJob

module.exports = {
    cron5(_session, _sessionkey, _number, _text){
        const job = new cron('*5 * * * * *', () => {
    console.log('cron5 eviado =', _session, _sessionkey, _number, _text)
    functions.enviarTexto(_session, _sessionkey, _number, _text)   
    },true, 'America/Sao_Paulo');
    },

    cron10(_session, _sessionkey, _number, _text){
        const job = new cron('*10 * * * * *', () => {
    console.log('cron10 eviado =', _session, _sessionkey, _number, _text)
    functions.enviarTexto(_session, _sessionkey, _number, _text)   
    },true, 'America/Sao_Paulo');
    },

    cron30(_session, _sessionkey, _number, _text){
        const job = new cron('*30 * * * * *', () => {
    console.log('cron30 eviado =', _session, _sessionkey, _number, _text)
    functions.enviarTexto(_session, _sessionkey, _number, _text)   
    },true, 'America/Sao_Paulo');
    }

}
*/

console.log('OI!')
